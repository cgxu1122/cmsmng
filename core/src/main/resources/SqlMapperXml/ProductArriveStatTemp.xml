<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ifhz.core.mapper.stat.ProductArriveStatTempMapper">
    <sql id="TABLE_NAME">TY_PRODUCT_ARRIVE_STAT_TEMP</sql>

    <resultMap id="ProductArriveStatTempResultMap" type="com.ifhz.core.po.stat.ProductArriveStatTemp">
        <id column="ID" property="id" jdbcType="BIGINT"/>
        <result column="PRODUCT_ID" property="productId" jdbcType="VARCHAR"/>
        <result column="GROUP_ID" property="groupId" jdbcType="BIGINT"/>
        <result column="CHANNEL_ID" property="channelId" jdbcType="BIGINT"/>
        <result column="UA" property="ua" jdbcType="VARCHAR"/>
        <result column="ARRIVE_DATE" property="arriveDate" jdbcType="DATE"/>
        <result column="CREATE_TIME" property="createTime" jdbcType="TIMESTAMP"/>

        <result column="TOTAL_NUM" property="totalNum" jdbcType="BIGINT"/>
        <result column="VALID_NUM" property="validNum" jdbcType="BIGINT"/>
    </resultMap>

    <insert id="insert" parameterType="com.ifhz.core.po.stat.ProductArriveStatTemp">
        insert into
        <include refid="TABLE_NAME"/>
        (
        ID,
        PRODUCT_ID,
        GROUP_ID,
        CHANNEL_ID,
        UA,
        ARRIVE_DATE,
        CREATE_TIME,
        TOTAL_NUM,
        VALID_NUM
        ) values (
        #{id,jdbcType=BIGINT},
        #{productId,jdbcType=BIGINT},
        #{groupId,jdbcType=BIGINT},
        #{channelId,jdbcType=BIGINT},
        #{ua,jdbcType=VARCHAR},
        #{arriveDate,jdbcType=DATE},
        #{createTime,jdbcType=TIMESTAMP},
        #{totalNum,jdbcType=BIGINT},
        #{validNum,jdbcType=BIGINT}
        )
    </insert>

    <select id="getById" resultMap="ProductArriveStatTempResultMap">
        select * from
        <include refid="TABLE_NAME"/>
        where ID = #{id}
    </select>
</mapper>